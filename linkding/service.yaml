apiVersion: v1
kind: Service
metadata:
  labels:
    app: linkding
  name: linkding
  namespace: default
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 9090
  selector:
    app: linkding
  type: LoadBalancer
